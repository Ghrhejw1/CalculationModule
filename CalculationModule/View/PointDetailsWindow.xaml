<Window x:Class="CalculationModule.View.PointDetailsWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CalculationModule.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800" Background="AliceBlue"
      Title="PointDetailsWindow">

    <Grid Margin="0,10,10,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Название:" Grid.Row="0"/>
        <TextBox x:Name="NameTextBox" Grid.Row="0" Margin="72,0,10,10" IsReadOnly="True"/>

        <Label Content="Широта:" Grid.Row="1"/>
        <TextBox x:Name="LatitudeTextBox" Grid.Row="1" Margin="72,5,10,5" IsReadOnly="True"/>

        <Label Content="Долгота:" Grid.Row="2"/>
        <TextBox x:Name="LongitudeTextBox" Grid.Row="2" Margin="72,5,10,5" IsReadOnly="True"/>
        </Grid>
</Window>
